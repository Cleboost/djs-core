---
"@djs-core/testing": major
"@djs-core/dev": minor
"@djs-core/runtime": patch
---

Add `@djs-core/testing` with mock helpers for commands, components, events, and autocomplete. Ship it via `@djs-core/dev`, add `djs-core test`, and export ButtonHandler/SelectMenuHandler from runtime.
