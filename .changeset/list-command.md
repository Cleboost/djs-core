---
"@djs-core/dev": minor
---

Add `djs-core list` command that scans `src/` and prints all detected commands, context menus, buttons, select menus, modals, events and cron tasks with their routes and file paths.
