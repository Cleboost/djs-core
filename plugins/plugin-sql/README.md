# @djs-core/plugin-sql

> **Deprecated** — use native `db:` in `djs.config.ts` and `client.db` instead.
> See [migration guide](https://djs-core.cleboost.com/guides/migrate-db-plugins).

Minimal raw SQL on Bun SQLite via tagged templates on `client.sql`.
