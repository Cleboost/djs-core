{
  "name": "djs-core",
  "version": "5.0.2",
  "type": "module",
  "bin": {
    "djs-core": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsup src/**/*.ts --format esm --dts --clean --outDir dist --minify"
  },
  "peerDependencies": {
    "discord.js": "^14.21.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "discord.js": "^14.21.0",
    "typescript": "^5"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "inquirer": "^12.7.0",
    "tsup": "^8.5.0"
  }
}
