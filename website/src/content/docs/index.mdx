---
title: "djs-core"
icon: "house"
description: "Type-safe Discord bots with file-based routing on top of discord.js."
---

import CardGroup from '@components/CardGroup.astro';
import Card from '@components/Card.astro';

**djs-core** is a framework on top of [discord.js](https://discord.js.org) that kills the boring scaffolding. Drop files in folders — commands, buttons, events register themselves. Options are typed. Database is built in.

## Philosophy

**File-based, not config-based.** Your project structure *is* your bot structure. No manual registration, no index files to update.

**Typed by default.** Declare command options on the builder — the `options` object in `.run()` is fully inferred.

**Built for the long run.** `djs-core check` catches deprecated patterns. `djs-core generate` scaffolds files the linter won't hate.

## What it handles

- Slash commands, context menus, autocomplete
- Buttons, modals, all select menu types
- Events and cron tasks
- Native Drizzle database — `client.db`, `djs-core db` CLI
- Third-party plugins via `definePlugin`

## Packages

<CardGroup cols={2}>
  <Card title="@djs-core/runtime" icon="npm" href="https://npmjs.com/package/@djs-core/runtime">
    Core runtime — loads and runs your bot.
  </Card>
  <Card title="@djs-core/dev" icon="npm" href="https://npmjs.com/package/@djs-core/dev">
    CLI — dev, build, check, generate, test, db.
  </Card>
  <Card title="@djs-core/db" icon="database" href="/essentials/database">
    Drizzle layer — `client.db` and migrations.
  </Card>
  <Card title="@djs-core/testing" icon="flask" href="/essentials/testing">
    Mock helpers for `bun:test` — ships with dev.
  </Card>
</CardGroup>

## Get started

<CardGroup cols={2}>
  <Card title="Installation" icon="download" href="/getting-started/installation">
    Set up a bot from template or scratch.
  </Card>
  <Card title="Routing" icon="route" href="/guides/routing">
    How file paths become commands and customIds.
  </Card>
  <Card title="Database" icon="database" href="/essentials/database">
    Native Drizzle — no plugin required.
  </Card>
  <Card title="CLI Reference" icon="terminal" href="/guides/cli">
    Every `djs-core` command in one page.
  </Card>
</CardGroup>
